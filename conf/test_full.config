/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run nf-core/pacvar -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '6.h'
    ]
}

params {
    config_profile_name         = 'Test profile'
    config_profile_description  = 'Minimal test dataset to check pipeline function for pure-trgt workflow'
    input                       = params.pipelines_testdata_base_path + 'refs/heads/pacvar/pacbio_data/samplesheet_full.csv'
    genome                      = 'GATK.GRCh38'
    snv_caller                  = 'gatk4'
    intervals                   = params.pipelines_testdata_base_path + 'refs/heads/pacvar/pacbio_data/c9orf72-short.bed'
    workflow                    = 'wgs'
    skip_demultiplexing         = true
}
